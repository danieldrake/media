<%= form_for(@confrence) do |f| %>
  <% if @confrence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@confrence.errors.count, "error") %> prohibited this confrence from being saved:</h2>

      <ul>
      <% @confrence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :year %><br />
    <%= f.text_field :year %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
